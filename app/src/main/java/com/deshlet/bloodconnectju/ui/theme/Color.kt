package com.deshlet.bloodconnectju.ui.theme

import androidx.compose.ui.graphics.Color

// Converted 1:1 from the web app's OKLCH brand tokens
// (resources/css/app.css) via the standard OKLab/OKLCH -> sRGB formula —
// not eyeballed, so these are the actual same colors, not an approximation.
// The web app has no dark-mode palette, so there's only one scheme here too.

val BcBackground = Color(0xFFFBFAF7)
val BcForeground = Color(0xFF231C19)
val BcSurface = Color(0xFFF6F3EF)
val BcCard = Color(0xFFFFFFFF)

val BcPrimary = Color(0xFFC92F30)
val BcPrimaryForeground = Color(0xFFFEFBF8)

val BcSecondary = Color(0xFFF3EDE9)
val BcSecondaryForeground = Color(0xFF352C28)

val BcMuted = Color(0xFFF3EFEC)
val BcMutedForeground = Color(0xFF736A64)

val BcAccent = Color(0xFFF8E6E4)
val BcAccentForeground = Color(0xFF901117)

val BcDestructive = Color(0xFFC92F30)
val BcDestructiveForeground = Color(0xFFFEFBF8)

val BcSuccess = Color(0xFF489A68)
val BcWarning = Color(0xFFDEA143)
val BcInfo = Color(0xFF4381AA)

val BcBorder = Color(0xFFE2DDD9)
