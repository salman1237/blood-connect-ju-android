<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".BloodConnectApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BloodConnectJU">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.BloodConnectJU"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".notifications.BloodConnectFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Default notification channel FCM falls back to when a message
             doesn't specify one — matches the channel this app actually
             creates and posts to (see BloodConnectFirebaseMessagingService). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="blood_requests" />

        <!-- pusher-websocket-android (used only for its Pusher-protocol
             WebSocket client, see RealtimeService) bundles a legacy FCM
             Instance ID service for Pusher's own separate push-notification
             feature, which this app doesn't use — the manifest merger
             rejects it outright since it has no android:exported set,
             which every modern targetSdk requires on a component with an
             intent-filter. Removed rather than fixed upstream since it's
             genuinely unused dead weight for this app's purposes. -->
        <service
            android:name="com.pusher.android.notifications.fcm.FCMInstanceIDService"
            tools:node="remove" />
    </application>

</manifest>