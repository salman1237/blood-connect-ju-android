<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive icon background layer: the app's own off-white brand
     background color (BcBackground, matching web's CSS background token),
     not the default Android Studio template's green grid. The foreground
     logo mark (a red blood-drop with a heart) was generated against this
     same cream tone, so this keeps the reconstructed adaptive icon visually
     matching the source artwork rather than clashing with an arbitrary
     background color. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FBFAF7"
        android:pathData="M0,0h108v108h-108z" />
</vector>
