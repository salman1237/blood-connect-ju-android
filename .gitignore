*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Firebase — google-services.json is generally safe to commit per Google's
# own guidance (it's a project identifier, not a secret; access is scoped
# by package name + API restrictions), but this repo is public and the
# safer default for a public repo is to keep it local-only.
app/google-services.json
