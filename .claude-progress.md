# Blood Connect JU — Android — Progress Log

Native Android companion app for [Blood Connect JU](https://blood.deshlet.com) (Jahangirnagar University's blood donation coordination platform). Same Laravel backend as the web app — this project talks to it over a new versioned JSON API (`/api/v1/...`, Laravel Sanctum token auth) added to that repo rather than a separate backend or a rewrite.

Stack: Kotlin, Jetpack Compose (Material 3), Hilt, Retrofit + OkHttp + kotlinx.serialization, Navigation Compose, DataStore. `minSdk 24` / `compileSdk 37` / `targetSdk 37`. Package `com.deshlet.bloodconnectju`.

Same phased, checkpoint-based cadence as the Laravel repo: build a phase, verify it for real (compile, install, run — not just "looks right"), commit, push.

## Phase 1 — Project scaffold, auth API loop, and brand theme ✅ (2026-08-24)

First working slice: prove the whole chain — Android app → Retrofit → the backend's new `/api/v1` auth endpoints (shipped the same day as [Phase 19 on the Laravel side](../Blood_Connect_JU/.claude-progress.md)) → a real account — actually works end to end, not just compiles.

- **Project created** via Android Studio's Compose "Empty Activity" template. `minSdk` deliberately set to 24 (Android Studio's own default, based on live Play Store distribution data) rather than a higher floor picked for no real reason — nothing in this stack (Compose, Retrofit, Hilt, Coroutines, DataStore) needs anything newer, and this is a university donor app where excluding older/budget devices for no technical reason works against the whole point of the app.
- **Toolchain compatibility fixes** — the bundled toolchain (AGP 9.3.2, Kotlin 2.2.10, Gradle 9.5) is new enough that two real ecosystem gaps showed up before any app code was even involved, both found by actually running `gradlew assembleDebug` rather than assuming a fresh Android Studio project just builds:
  - Hilt's Gradle plugin needed bumping past whatever Android Studio/most docs currently reference to the actual latest published release (2.60.1) — anything older doesn't know about AGP 9's plugin API.
  - AGP 9's new "built-in Kotlin" mode conflicts with how KSP (Hilt's annotation processor) registers its generated-sources directories via the legacy `kotlin.sourceSets` DSL. Fixed via `android.disallowKotlinSourceSets=false` in `gradle.properties` — Gradle's own build-failure message names this exact flag as the documented escape hatch.
- **Auth API client**: `ApiService` (Retrofit interface mirroring `routes/api.php`'s `/api/v1` group exactly), DTOs matching `UserResource`'s JSON field names 1:1, `TokenStore` (DataStore-backed Sanctum token persistence, with an in-memory cache so the OkHttp auth interceptor isn't blocking on disk I/O per request), `AuthRepository` (register/login/logout/me, decodes Laravel's `{message, errors}` validation-error shape into per-field errors), all wired through Hilt (`NetworkModule`).
- **Screens**: Login, Register (chip-based role/gender pickers rather than a dropdown, to sidestep a real Material3 API-surface risk — `ExposedDropdownMenuBox`'s `menuAnchor()` signature has changed across Compose versions and this Compose BOM is very recent), Home (calls the authenticated `/api/v1/user` to prove the token round-trips). Navigation Compose picks the start destination from whether a token is already stored.
- **Brand theme** — the first build used the wizard's default Material3 theme untouched, including `dynamicColor = true`, which on Android 12+ silently overrides any color scheme with one derived from the device wallpaper (Material You). That's the actual root cause of the app looking like generic Material Design instead of the brand on first run, not a missing-styles problem. Fixed by:
  - Converting the web app's actual OKLCH brand tokens (`resources/css/app.css`) to sRGB hex via the real OKLab/OKLCH→sRGB math (not eyeballed) and applying them as an explicit `lightColorScheme` with `dynamicColor` off entirely.
  - A `Shapes` scale anchored on the web's `--radius: 0.75rem` token.
  - Public Sans (same font as web) bundled locally as a single variable font file (`res/font/public_sans.ttf`, one file covers the whole weight axis via `FontVariation`) rather than fetched through the Downloadable Fonts provider — that path was tried first and found to silently fall back to the system font on any device without a signed-in Google account (confirmed via logcat: `No resolver, short-circuit {Public Sans...} to failure`), which is a real-world reliability gap, not just an emulator quirk.
- **Verification**: every fix in this phase was confirmed by actually running `gradlew assembleDebug` and reading the real failure, not guessed at — this caught a wrong Retrofit converter package (`com.jakewharton.retrofit2.converter...`, not `retrofit2.converter...`), a wrong `Spacer` import (`foundation.layout`, not `material3`), and a missing experimental-API opt-in, in addition to the two toolchain issues above. The final APK was installed on a real running emulator via `adb`, launched, and screenshotted to confirm the actual rendered UI — both before the theme fix (confirming the dynamic-color bug) and after (confirming the real brand colors, shapes, and font all render correctly).
- **Not built yet**: Google Sign-In (needs a new backend endpoint to exchange a Google ID token for a Sanctum token — not just Android-side work), onboarding, donor profile, requests, matching, notifications, donor directory, leaderboard. All later phases.

## Phase 2 — Onboarding wizard + donor profile ✅ (2026-08-24)

Same 4-step flow as the web onboarding wizard (`resources/views/onboarding.blade.php`): blood group, role/gender/DOB, hall/batch/department/phone/WhatsApp, availability/last-donation-date. Navigation now routes a fresh account straight into it and won't reach Home until it's done.

- **Backend additions** (same repo/commit boundary as the web app, not duplicated logic): `GET /api/v1/meta` (halls/departments/blood groups/batches — same `config/juniv.php` source as the web view) and `PATCH /api/v1/donor-profile`, which reuses `App\Http\Requests\UpdateDonorProfileRequest` and `User::updateDonorProfile()` directly — the exact same FormRequest and model method the web onboarding wizard and profile-edit page already share, so mobile can't drift from web's validation rules the way a hand-rolled duplicate would risk. 6 new backend tests (237 passing total), verified with real `curl` round-trips against a local server (register → meta → donor-profile update → `has_completed_onboarding: true`) before ever touching the Android side.
- **`ProfileRepository`** — meta fetch + donor-profile update, decoding Laravel's validation-error shape the same way `AuthRepository` does. That parsing logic was pulled out into a shared `parseApiError()` helper (`data/remote/ApiErrors.kt`) during this phase so the two repositories don't carry two independently-maintained copies of it.
- **`OnboardingScreen`** — local step state (1–4) matching the web wizard's steps/titles/subtitles/progress bar exactly, pre-filled from the account's existing role/gender/DOB (already collected at registration) via one `/api/v1/user` call on entry. Hall/batch/department use a hand-rolled `SimpleDropdownField`/`GroupedDropdownField` (`ui/components/Dropdowns.kt`) — a plain `Box` + `DropdownMenu` rather than `ExposedDropdownMenuBox`, sidestepping that component's `menuAnchor()` API instability across recent Compose versions (flagged as a risk in Phase 1, avoided here the same way Register's chip-based pickers avoided it there).
- **Navigation**: login/register success now branches on `has_completed_onboarding` (present in their own response — no extra round trip) to route straight to Onboarding or Home; cold start with a stored-but-unverified token now makes one `/api/v1/user` call to decide the same way. `HomeScreen` shows the completed donor profile (blood group, department/hall) instead of the placeholder "not finished yet" text from Phase 1.
- **Verification gap, disclosed rather than glossed over**: the backend contract was fully verified end-to-end via real HTTP calls, and the build compiles clean, but the final live click-through *on the emulator* wasn't completed this phase — chasing an unrelated emulator networking glitch (`Active default network: none`, fixed itself after a cold restart) led to a `System UI isn't responding` crash-loop on that AVD instance that isn't fixable without GUI access to dismiss the dialog. The registration screen itself was confirmed working through real form input and submission up to that point (chip selection, validation states, keyboard handling all correct on-device). **Next session should do one live click-through of the onboarding wizard on a fresh emulator or a real device before building on top of it.**

**Follow-up on the emulator issue above**: root-caused, not just retried — the host had only ~2GB of 16GB RAM free (Android Studio + the emulator + accumulated Gradle daemons), which is what was actually causing the AVD's system processes to ANR, not the app or the earlier networking glitch (that part turned out to be a red herring — it cleared on its own after a cold restart). Stopping the Gradle daemon (`gradlew --stop`) barely moved the number, confirming Android Studio/the emulator itself were the real consumers. Killed the emulator to free the RAM rather than leave it stuck. **User decision: skip forcing a live click-through this session, proceed to Phase 3 build-verified only, revisit on-device verification (fresh emulator with more headroom, or a real device) when convenient.**

## Phase 3 — Requests: dashboard feed, create, detail, matching donors ✅ (2026-08-25)

Backend landed same-session as [blood-connect-ju-laravel Phase 21](../Blood_Connect_JU/.claude-progress.md) — verified via real `curl` end to end before any Android code touched it, same discipline as Phase 2.

- **`RequestRepository`** — list/stats/get/matchingDonors (plain fetch-or-null, same pattern as `ProfileRepository.meta()`) plus `create()` (same `RequestResult`/`parseApiError()` pattern as `AuthRepository`/`ProfileRepository`).
- **`RequestsScreen`** — the feed: stat tiles (active/critical/registered donors), a blood-group filter chip row, urgency-then-recency request cards. Reachable from `HomeScreen` via a "Blood requests" button — deliberately not wired into a bottom-nav/drawer shell yet, since that's its own scope once there's enough sections to actually need one (donor directory, leaderboard, profile are all still ahead).
- **`CreateRequestScreen`** → on success navigates to **`MatchingDonorsScreen`**, popping back to the feed underneath it (mirrors the web app's own post-create redirect straight to matching donors, not back to the request's own detail page).
- **`RequestDetailScreen`** — read-only for now: full details plus a responses list. No "I can donate" / fulfill actions yet — those are Phase 4 (response/matching + notifications) per the original roadmap, not an oversight here.
- **`UrgencyBadge`/`StatusPill`** (`ui/components/Badges.kt`) — same labels and colors as the web components (`urgency-badge.blade.php`/`status-pill.blade.php`), including the pulsing dot on "Critical". Needed one new color token, `BcWarningForeground`, that Phase 1's OKLCH conversion pass hadn't pulled over yet since nothing used it until now.
- Two experimental-API opt-ins needed (`@file:OptIn(ExperimentalMaterial3Api::class)` for `TopAppBar`, same class of issue as `FontVariation` in Phase 1) — found by actually compiling, not guessed at up front.
- Build verified clean (`gradlew assembleDebug`). Live on-device click-through not attempted this phase either, per the same-session decision above — carries forward as the same open item, now covering Phases 2 and 3 together.

## Live verification, and a real theme bug it caught ✅ (2026-08-25)

The emulator came back healthy later the same session — RAM pressure had eased — so the full Phase 2 + Phase 3 click-through deferred above actually happened: register → all 4 onboarding steps (blood group grid, role/gender/DOB prefill, both custom dropdowns, WhatsApp toggle, availability) → Home showing the completed donor profile → requests feed with real stats/filters → request detail → create a request → matching donors with ranked results and working WhatsApp buttons. Every screen built in Phases 2–3 confirmed live against production, not just backend- and build-verified.

It also caught a real bug the backend verification couldn't have: every `Card` (stat tiles, request cards, donor rows) rendered with a visible lavender/purple tint instead of the brand's neutral off-white. Root cause — `lightColorScheme()` fills in any role you don't pass explicitly with Material3's own baseline purple seed palette, and `Card` defaults to the `surfaceContainer*` roles for its background, none of which Phase 1's theme pass had set (it only covered the roles that were *visibly* wrong at the time — primary/secondary/background/surface/error/outline). Fixed by setting every remaining `ColorScheme` role explicitly in `Theme.kt`, mapped onto the existing brand colors (no new ones needed) rather than left to fall back. Confirmed fixed with a second live pass — cards now render in the correct neutral tone.

**Lesson for future phases**: a `lightColorScheme()`/`darkColorScheme()` call needs *every* role set explicitly up front, not just the ones a first screen happens to exercise — an unset role doesn't fail loudly, it just quietly renders Material's default color instead of the brand's.

## Phase 4a — Respond / confirm / fulfill / mutual confirmation ✅ (2026-08-25)

Push notifications (the other half of Phase 4 from the original roadmap) are split into their own later phase — they need a Firebase project created first, not something buildable from the Android/API side alone. This slice is everything else: the full response lifecycle.

- Backend landed same-session ([blood-connect-ju-laravel Phase 22](../Blood_Connect_JU/.claude-progress.md)) — `BloodRequestResource` now carries authoritative `can_respond`/`can_fulfill`/`can_confirm`/`can_confirm_donation` flags (the same Policy checks the web buttons are gated on), so `RequestDetailScreen` never reimplements that logic in Kotlin — it just renders what the server says is allowed.
- `RequestRepository` gained `respond()`/`fulfill()`/`confirmResponse()`/`confirmDonation()`, all sharing one `runAction()` private helper (same `RequestResult`/`parseApiError()` shape `create()` already used, pulled out rather than copy-pasted four times).
- `RequestDetailScreen`: an "I can donate" button when `can_respond`, a status-advance button when `can_fulfill` (label switches between "Mark donor found" / "Mark fulfilled" depending on current status), and per-response "Confirm this donor" / "Confirm the donation happened" buttons gated the same way. All four actions reload the request from the action's own response afterward — no separate re-fetch needed, since every endpoint already returns the updated resource.
- Build verified clean. **Live-verified on-device against production** with a real account: registered fresh, completed onboarding (including catching and correctly recovering from a real server-side validation error — submitted step 4 without a required Hall, got "The hall field is required." back and displayed correctly, went back and fixed it), opened an existing real request, confirmed the "I can donate" button renders correctly and is only shown because this account's blood group is actually compatible — matching `can_respond`'s server-side computation. Deliberately did **not** tap it, since that request belongs to the account owner's real production data, not a throwaway — the underlying action itself was already proven correct via full curl lifecycles (both local and production) before this on-device pass, so tapping it would have added data-pollution risk for no new verification value.
- Also incidentally surfaced a minor, non-blocking edge case worth a note for later: if a stored token belongs to an account that no longer exists (e.g., manually deleted from the database, as happens after every one of these verification passes' cleanup), `/api/v1/user` fails and the app currently falls back to routing to Onboarding rather than Login. Reasonable enough as a default (a real expired/invalid token in production would show a clear error on submit rather than silently signing someone out from a transient network blip), but distinguishing "token is actually invalid" from "network hiccup" would be a cleaner fix — not addressed this phase, flagged for whenever `BloodConnectNavHost` is next touched.

## Phase 4b — Push notifications (FCM client) ✅ (2026-08-25)

Backend half landed same-session ([blood-connect-ju-laravel Phase 23](../Blood_Connect_JU/.claude-progress.md)): `FcmSender`/`FcmChannel`/`push_tokens` table, `POST`/`DELETE /api/v1/push-tokens`, wired into `RequestResponded`/`DonorSelected`/`NewMatchingRequest`. This phase is the Android side that actually receives them, built after the user walked through creating the Firebase project (`blood-connect-ju-6baf2`) and handing over `google-services.json` (`app/google-services.json`, gitignored — see below).

- **Gradle**: `com.google.gms.google-services` plugin (4.5.0) + `firebase-bom` (34.18.0) + `firebase-messaging` + `kotlinx-coroutines-play-services` (for `.await()` on Firebase's `Task<T>`). Verified real current versions against Google's Maven repo directly (`dl.google.com/android/maven2/.../maven-metadata.xml`) rather than guessed — caught that `firebase-messaging-ktx` no longer exists as a separate artifact in recent BOMs (the KTX extensions merged into the base `firebase-messaging` artifact); using the old `-ktx` coordinate fails resolution with an empty version.
- **`PushTokenRepository`** — `syncCurrentToken()` (fetch the current FCM token, register it), `unregisterCurrentToken()` (called from `AuthRepository.logout()` *before* the local Bearer token is cleared, since unregistering still needs it to authenticate). Every method best-effort/`runCatching` — a failed sync just means this device misses pushes until the next successful login, never worth surfacing as a user-facing error.
- **`AuthRepository`** wired to call `syncCurrentToken()` right after a successful login/register (the token is already cached by the Firebase SDK by then even if it was first generated before the user ever logged in, so nothing is lost by not syncing earlier).
- **`BloodConnectFirebaseMessagingService`** (`@AndroidEntryPoint`) — `onNewToken()` re-syncs on rotation; `onMessageReceived()` builds the notification by hand (title/body from the FCM payload, `request_id` from the data payload as a `PendingIntent` extra into `MainActivity`) for the case FCM actually calls into app code: **only when the app is foregrounded**. When backgrounded, FCM auto-displays the notification straight from system code using the manifest's `default_notification_channel_id` meta-data and attaches the `data` payload as Intent extras automatically on tap — no app code runs for that path at all, which is why `MainActivity`'s extra-reading has to handle both origins identically.
- **Deep-linking**: `MainActivity` reads a `request_id` extra (from either origin above) into a `mutableStateOf` property, handling both cold start (`onCreate`) and a tap while already running (`onNewIntent`, needing `android:launchMode="singleTop"` in the manifest so re-launching doesn't stack a duplicate Activity). `BloodConnectNavHost` takes that as a parameter and navigates to the request-detail route via a `LaunchedEffect` gated on `resolvedStart == Routes.HOME` — deliberately **not** valid during Onboarding, since none of the three wired notifications can fire for an account that hasn't finished onboarding yet.
- **Runtime permission**: `POST_NOTIFICATIONS` (Android 13+) requested unconditionally on `MainActivity.onCreate()` via `ActivityResultContracts.RequestPermission()` — a no-op on older APIs where the permission doesn't exist, and denial just means no pushes show (same best-effort philosophy as the rest of this phase).
- **Two soft-deprecation warnings investigated, not blindly suppressed**: `FirebaseMessaging.getToken()`/`FirebaseMessagingService.onNewToken()` are marked `@deprecated` in the actual SDK source (confirmed by pulling `FirebaseMessaging.java`/`FirebaseMessagingService.java` from `firebase/firebase-android-sdk` on GitHub, not guessed from the compiler warning text alone) in favor of a newer `register()`/`onRegistered(installationId)` model built around Firebase Installation IDs instead of a directly-readable token — but that model is opt-in (a manifest flag), changes the whole callback shape, and isn't what current mainstream Firebase documentation uses for a standard FCM setup. Deliberately not migrated to this phase; `@Suppress("DEPRECATION")` added with a comment explaining why, rather than either ignoring the warning silently or chasing an experimental API with unclear backend implications.
- **Real bug found via live push, not caught by any test or compile check**: the `blood_requests` notification channel was only ever created lazily inside `onMessageReceived()` — which never runs for a backgrounded app receiving a message with a `notification` block (that's exactly the path FCM auto-displays through, bypassing app code entirely). First live push landed correctly but under FCM's generic `fcm_fallback_notification_channel` ("Miscellaneous"), grouped under a bare "Android System" summary instead of showing as "Blood Connect JU". Fixed by extracting channel creation into `ensureBloodRequestsNotificationChannel()` (`notifications/NotificationChannels.kt`) and calling it eagerly from `BloodConnectApplication.onCreate()`, so the channel exists before any push can possibly arrive — confirmed via `adb shell dumpsys notification` before (channel absent, fallback used) and after (channel present at app launch, real push correctly shows under "Blood Connect JU" with the right title/body).
- **Fully live-verified end-to-end on a real physical device** (not the emulator — a real device is a more faithful FCM test since it has genuine Play Services), against production, for real: registered a fresh account through the actual UI → app called `POST /api/v1/register` → `PushTokenRepository` fetched a real FCM token and called `POST /api/v1/push-tokens` (201) → confirmed via SSH `tinker` that the token landed correctly in production's `push_tokens` table (a genuine 142-char FCM token, not a stub) → backgrounded the app → sent a real push from a **production** `artisan tinker` session through the actual `FcmSender` service class (not a test double) → watched it land in the system tray with the correct title, body, app icon, and (after the channel fix) the correct named channel → tapped it → confirmed `MainActivity` received the `request_id` extra and the deep-link gate correctly held back navigation because the test account hadn't completed onboarding (proving the safety branch works, not just the happy path). Test user and its push-token row deleted from production afterward; the debug APK uninstalled from the borrowed device afterward too.
- uiautomator-based text entry hit real friction this phase (fast sequential `input tap`+`input text` calls occasionally dropped/garbled characters or mistargeted a field once the on-screen keyboard shifted the layout, and Android's back button pops Compose Navigation's back stack rather than just dismissing the keyboard when nothing is left to un-focus) — worked around by reading back each field's actual state via `uiautomator dump` after typing it rather than trusting the input call succeeded, and by dismissing the keyboard via the IME's own action key instead of `KEYCODE_BACK`. Noting this here since it's a real gotcha for any future on-device UI automation on this project, not just this phase.
- **`app/google-services.json`** — gitignored rather than committed (a deliberate, disclosed choice: Google's own guidance says it's generally safe to commit since it's a project identifier rather than a secret, but this repo is public and the safer default was chosen). Anyone building this repo fresh needs to place their own copy there first.

## Phase 5 — Donor directory ✅ (2026-08-25)

Backend landed same-session ([blood-connect-ju-laravel Phase 25](../Blood_Connect_JU/.claude-progress.md)) — verified via curl before this phase touched it, same discipline as every prior phase.

- **`DonorRepository`** — `list(search, bloodGroup, hall)` / `get(id)`, same plain fetch-or-null shape as `ProfileRepository`/`RequestRepository`.
- **`DonorDirectoryScreen`** — search field (350ms debounced — the first debounce in this app; every other filter here is a chip tap, not free text, so nothing else needed one) + blood-group filter chips (same `FilterChip` row pattern as `RequestsScreen`) + a donor list.
- **Extracted `DonorRow` into a shared component** (`ui/components/DonorRow.kt`) — it was a private composable duplicated conceptually between here and `MatchingDonorsScreen` (Phase 3); pulled out once a second real call site needed it, with an optional `onClick` (directory: opens the profile; matching donors: still just the WhatsApp button, no navigation).
- **`DonorDetailScreen`** — header card (avatar circle, name, hall, Available/Eligible pills, trust score, WhatsApp button), details grid (gender/role/age/hall/batch/department/phone), badges row (hidden entirely when empty, matching the web page), donation history list with its own empty state.
- **Real layout bug found via live device testing, not caught by compiling**: the details grid's `DetailRow` used `Arrangement.SpaceBetween` on a plain `Row` — fine for short values, but a long wrapped value (e.g. "Institute of Information Technology" wrapping to two lines) had no reserved gap from its label, so the label and the wrapped line's first word visually ran together ("DepartmentInstitute of Information"). Fixed by giving the label and value each a `weight()` with `Arrangement.spacedBy(12.dp)` between them and right-aligning the value — confirmed fixed with a second live screenshot against the exact same real donor record.
- Home gained a "Donors" button (`onViewDonors`) alongside the existing "Blood requests" one — still no bottom-nav/drawer shell, consistent with the "later phase, once there's enough sections to need one" note from Phase 3.
- Build verified clean (zero warnings). **Fully live-verified on a real physical device against production**: registered and onboarded a fresh account through the actual UI (including exercising every hall/batch/department `DropdownMenu` live — confirming Phase 2's dropdown component still works correctly end-to-end), opened the directory (real `GET /api/v1/donors` call, real seeded donors rendered with correct blood group/department/WhatsApp button), tapped the "A+" filter chip and confirmed the list correctly narrowed to only A+ donors, tapped into a real donor's profile (real `GET /api/v1/donors/{id}` call) and confirmed the full detail screen — including the badges-hidden-when-empty case and the donation-history-empty-state case on a second donor. Test account and its push-token row deleted from production afterward; debug build uninstalled from the device afterward too.
- **Environment note, not a code issue**: this phase's live verification hit two dead ends before succeeding — the emulator came up with `wlan0` in `NO-CARRIER` state (no network at all, silently emptying every dropdown's options and making them look broken when they weren't), and the borrowed physical device disconnected mid-session and had to be waited out. Neither was an application bug; recorded here in case a future session hits the same "dropdown looks unresponsive" symptom and wastes time suspecting the component instead of checking `ip addr show wlan0` / device connectivity first.

## Phase 5 follow-up — real WhatsApp glyph instead of text ✅ (2026-08-25)

User feedback: the WhatsApp buttons said the word "WhatsApp" instead of showing the icon the web app uses. Matched web exactly rather than inventing an Android-specific treatment:

- **`res/drawable/ic_whatsapp.xml`** — the same official WhatsApp glyph path web's `x-whatsapp-icon` component uses (simple-icons project), transcribed verbatim from the SVG `d` attribute, not redrawn.
- **`DonorRow`** (list rows — directory + matching donors) — was a `Button` with the literal text "WhatsApp"; now an icon-only circular button (36dp, WhatsApp brand green `#25D366`), matching web's `size-9 rounded-full bg-[#25D366]` row treatment exactly.
- **`DonorDetailScreen`**'s big CTA — kept its text (web's own detail-page button keeps "Message on WhatsApp" too, unlike the icon-only list rows) but now shows the icon in front of it and uses the same WhatsApp green background instead of the default Material button color.
- WhatsApp green defined as a local one-off color constant in each file, not added to the shared brand token set — matching web's own choice to keep it as an inline arbitrary value rather than a design-system color.
- Build verified clean. Live-verified against production: registered a fresh account (this time via a fast `curl` register + onboard, only the login form itself driven through the UI, since the full onboarding wizard's already been proven multiple times), confirmed the real glyph renders correctly in both the row circles and the detail page's full-width button. Test account cleaned up afterward.

## Phase 6 — Leaderboard ✅ (2026-08-25)

Backend landed same-session ([blood-connect-ju-laravel Phase 26](../Blood_Connect_JU/.claude-progress.md)) — verified via curl before this phase touched it.

- **`LeaderboardRepository`** — one method, `get()`, same fetch-or-null shape as every other repository.
- **`LeaderboardScreen`** — ranked list, rank badge (top-3 in brand primary red/white, 4+ in muted secondary — same visual split as web's `$loop->index < 3` check), group name, donation count with singular/plural text ("1 donation" vs "N donations"), same empty state copy as the web page ("No confirmed donations yet…").
- Rank is derived client-side from list position (`itemsIndexed`), matching how the backend resource deliberately doesn't send a `rank` field — the same choice web's Blade view already made with `$loop->iteration`.
- Home gained a third button, "Leaderboard", alongside "Blood requests" and "Donors".
- Build verified clean. **Fully live-verified on a real device against production**: real `GET /api/v1/leaderboard` call, real seeded hall/department donation counts rendered in the correct descending order with the correct top-3 styling and correct singular/plural donation text. Test account cleaned up afterward.

## Phase 7 — Profile / account editing ✅ (2026-08-25)

Backend landed same-session ([blood-connect-ju-laravel Phase 27](../Blood_Connect_JU/.claude-progress.md)) — `PATCH /api/v1/profile` (name/email, resets `email_verified_at` when the email actually changes) and `DELETE /api/v1/profile` (password-gated, revokes every token, deletes the user), verified via curl before this phase touched it.

- **`ProfileViewModel`/`ProfileScreen`** — one screen, three cards: **Account** (name/email + "Save account details", "Not yet verified" hint under an unverified email), **Donor profile** (the full flattened onboarding field set — blood group chips, role chips gated by `user.canSelfServiceRole()`, gender chips, DOB, hall/batch/department dropdowns, phone, WhatsApp toggle, availability toggle, last-donation-date — reusing the same update flow Phase 2's onboarding wizard already proved), and **Danger zone** (password-confirm two-step "Delete account" with Cancel/Confirm-delete). Reachable from `HomeScreen` via a new "Profile" button.
- **Two real bugs found and fixed this phase, beyond the backend guard-name bug already caught and fixed before Android work started** (see the Laravel-side Phase 27 entry for `current_password:sanctum`):
  1. **Selected `FilterChip`s were visually invisible inside a `Card`.** Material3's default selected-chip colors come from `secondaryContainer`, which this app's `Theme.kt` maps to `BcSecondary` (`#F3EDE9`) — nearly identical to `Card`'s own `surfaceContainer` background (`BcSurface`, `#F6F3EF`). Every prior screen's chips happened to sit directly on the page background, so this never showed until Profile put chips inside a `Card` for the first time. Root-caused by comparing against web's actual CSS (`has-[:checked]:bg-accent`, not `secondary`) and fixed with a shared `selectedChipColors()` helper (`ui/components/ChipColors.kt`) mapping onto `BcAccent`/`BcAccentForeground` instead — applied to all 14 `FilterChip` call sites across the app (`RegisterScreen`, `CreateRequestScreen`, `OnboardingScreen`, `ProfileScreen`, `DonorDirectoryScreen`, `RequestsScreen`), not just the one that surfaced it, since the same latent bug was sitting in every other screen too.
  2. **Retrofit's `@DELETE` annotation rejects an `@Body` parameter outright** (`"Non-body HTTP method cannot contain @Body"`, thrown at call time, not compile time) — found live, on-device, via the account-deletion flow's own error UI after several silent "Confirm delete" taps produced no network activity. Fixed `deleteAccount()` by switching to `@HTTP(method = "DELETE", path = "profile", hasBody = true)`, Retrofit's documented escape hatch. While fixing it, found the **exact same bug had been sitting undetected since Phase 4b** in `unregisterPushToken()` — silently swallowed the whole time by `runCatching` in `PushTokenRepository.unregisterCurrentToken()`, so logout "worked" from the user's perspective but never actually revoked the device's push token server-side. Fixed both together.
- **Fully live-verified end-to-end on a real physical device against production**, including a full fresh-account pass specifically to close a verification gap the previous session had left open (donor-profile save had only been visually screenshotted, never confirmed round-tripping through the server): registered a brand-new account through the actual UI, completed all 4 onboarding steps, opened Profile, changed blood group (A+ → O−) and last-donation-date and toggled availability off, tapped "Save donor profile", saw the real "Donor profile updated." status message, navigated away and back into Profile (and to Home) and confirmed every changed value reloaded correctly from the server rather than just persisting in local Compose state. Then re-ran the Danger Zone delete flow on this same account (password-gated, correct-password path) and confirmed via SSH `tinker` against production that the user row and its tokens were actually gone, not just locally forgotten. Test account cleaned up (in this case, cleanup *was* the final verification step).
- **A genuine on-device friction point worth recording, distinct from prior phases' `input text` garbling**: this device has Google Autofill enabled, and Android's Autofill framework treats email + password + confirm-password as one linked dataset — the moment any password field gains focus, Autofill can pop a suggestion dropdown *directly below* it that shifts on-screen coordinates for whatever the automation script expected to tap next (e.g. a "Confirm password" field), silently overwriting freshly-typed text with a stale saved credential from an earlier phase's test account instead of registering the intended tap. Worked around by re-dumping the UI (not just reusing a screenshot's coordinates) immediately after any password-field focus change, explicitly dismissing the dropdown via back-press before proceeding, and re-verifying every field's actual on-screen text before submitting rather than trusting the typed sequence succeeded. Did not touch the device's system-wide Autofill setting, since that's a persistent setting on a borrowed device beyond this app's own scope.
- **Deliberately deferred**: avatar/photo upload — the web app doesn't have one either (no `avatar_url` field anywhere in `UserResource`), so there's nothing to build against; not a gap, just parity with what actually exists.
- Build verified clean (`gradlew :app:compileDebugKotlin` and `:app:assembleDebug`, zero warnings).

## Project status

Phases 1–3, 4a, 4b, 5 (plus its WhatsApp-icon follow-up), 6, and 7 complete and **fully verified live on-device against production**, including real push notifications delivered end-to-end through Firebase, a fully working donor directory + leaderboard, and full profile/account self-service (edit + delete, both password-gated where relevant): register/login → onboarding (if incomplete) → requests feed → create/detail/matching-donors → respond/confirm/fulfill/mutual-confirmation → push notifications → donor directory search/filter → donor profile (with the real WhatsApp brand icon) → leaderboard → profile/account editing and deletion. Next up: donation history & my-requests, in-app notification center, settings, and Google Sign-In.
